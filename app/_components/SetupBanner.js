export default function SetupBanner() {
  return (
    <div className="setup-banner">
      <strong>Read-only mode.</strong> Set the <code>GITHUB_TOKEN</code> env var on Vercel to enable
      adding notes, ticking off deliverables, and managing the roster. See README for the 2-minute setup.
    </div>
  );
}
